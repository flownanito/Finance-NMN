spring.application.name=financeadvisor
server.port=${PORT:8085}

# Railway Database / Local H2 fallback
spring.datasource.url=${DATABASE_URL:jdbc:h2:file:./data/financedb;CIPHER=AES;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE}
spring.datasource.driverClassName=${DATABASE_DRIVER:org.h2.Driver}
spring.datasource.username=${DATABASE_USER:sa}
spring.datasource.password=${DATABASE_PASSWORD:NmNF1nanc3Adv1s0r_2026! sa}

# H2 Console disabled in production for security
spring.h2.console.enabled=false

# JPA Settings
spring.jpa.database-platform=${DATABASE_DIALECT:org.hibernate.dialect.H2Dialect}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# JWT Configuration
jwt.secret=NmN-F1nanc3-Adv1s0r-S3cret-K3y-2026-J5on-W3b-T0k3n-256bit!
jwt.expiration=86400000

# Admin credentials
app.admin.username=flownanito
app.admin.password=Nano15jada

# Security headers
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false
